- name: frontend
  hosts: WEB
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: latest

    - name: run nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes